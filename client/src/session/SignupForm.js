import React from 'react';

/**  For later-- 
'Password must be at least 8 characters'
'Password must be less than 35 characters'
          */
const SignupForm = () => {
  return <div>SignupForm</div>;
};

export default SignupForm;
