import styled from 'styled-components';

export const InputErrorMessage = styled.div`
  color: red;
  font-size: 0.8rem;
  padding: 0.25rem 0.5rem 0.5rem; /* match input padding */
  text-align: right;
`;

export default InputErrorMessage;
